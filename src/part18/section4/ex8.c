/*
 *
 * Task:
 *		Write a complete description of the type of x as
 *		specified by each of the following declarations.
 *
 *		(a) char (*x[10])(int);
 *			x - array of pointers to functions. Each function takes int
 *			argument and returns a char.
 *
 *		(b) int (*x(int))[5];
 *			x - is a function with int argument that returns array of 5
 *			integers.
 *
 *		(c) float *(*x(void))(int);
 *			x - is a function with no arguments that returns pointer
 *			to a function with int argument that returns float.
 *
 *		(d) void (*x(int, void (*y)(int)))(int);
 *			x - is a function with two arguments, where first takes
 *			integer and second is a function y with int argument that
 *			returns nothing; x returns pointer to a function with int
 *			argument and no return.
 *
 * Date: 2025-08-27
 * Time: 02:10 PM
 *
 */
